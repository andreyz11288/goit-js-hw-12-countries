<div class="card">
    <div class="card-img-top">
        {{!-- <img src="{{sprites.front_default}}" alt="{{name}}"> --}}

        <div class="card-body">
            <h2 class="card-title">Имя: {{name}}</h2>
        </div>
    </div>
</div>